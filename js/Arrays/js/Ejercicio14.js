//Unir en una cadena
//Dado un arreglo de palabras, únelas en una sola cadena separada por comas.


let cadena=["hola chema","bienvenido a javascript"];

function unir(){
    cadena=cadena.join()
    console.log(cadena)
}
unir();
